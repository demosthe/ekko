
<% @user_tracks.each do |track| %>
  <p>
    <% embed_info = @client.get('/oembed',
                                url: track.uri,
                                maxheight: 130,
                                maxwidth: 500) %>
    <%= embed_info['html'].html_safe %>
  </p>
<% end %>
