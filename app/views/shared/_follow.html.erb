<%= form_tag(relationships_path, method: :post) do %>
  <%#= hidden_field_tag :followed_id, %>
  <%= hidden_field_tag :soundcloud_id, user.id %>
  <%= submit_tag "Follow", class: "btn btn-large btn-primary" %>
<% end %>
