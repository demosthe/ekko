<% user_by_soundcloud_id = User.find_by(soundcloud_id: user.id) %>
<% if user_by_soundcloud_id.nil? %>
  <%= render partial: "import", locals: {user: user} %>
<% else %>
  <%= render partial: "shared/follow_form", locals: {user: user_by_soundcloud_id} %>
<% end %>
